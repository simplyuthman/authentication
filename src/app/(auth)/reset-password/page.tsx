'use client';

import React from 'react';
import { AuthPageContainer } from '@/components/forms/AuthPageContainer';

export default function ResetPasswordPage() {
  return <AuthPageContainer initialView="reset-password" />;
}
